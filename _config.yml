theme: minima
title: Yukan's recipes
description: All my recipes will (eventually) be put up here!
author: Yukan Perumal
url: "https://uq-zhiyao.github.io"
baseurl: "/yukans-recipes"

# Explicit layout defaults so the site renders the same locally as it does
# on GitHub Pages (which auto-assigns these via the jekyll-default-layout plugin).
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
